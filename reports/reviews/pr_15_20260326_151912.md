# AI 代码审查报告 PR #15

**PR 标题**: Create 1111.html
**审核时间**: 2026-03-26 15:19:12

---

## 📝 GitLab MR 代码审核结果

### ✅ 优点
1. 基础HTML结构完整，适配了移动端viewport
2. 样式简洁清晰，代码缩进统一，可读性较好
3. 功能划分清晰，实现了需求的基础交互逻辑

### ⚠️ 问题/改进建议
| 严重程度 | 问题说明 | 位置 | 修复建议 |
|---------|---------|------|---------|
| 严重 | 文件命名无意义，使用全数字`1111.html`，不符合语义化命名规范 | 文件名 | 重命名为符合功能的语义化名称，例如`inventory-registration.html` |
| 严重 | 存在XSS安全漏洞：直接拼接用户输入到`innerHTML`，用户输入恶意脚本可执行 | JS中`addSku`和`showTotal`方法的日志拼接 | 不要直接使用innerHTML拼接用户输入，改用`textContent`添加文本，或对用户输入做转义后再插入 |
| 严重 | 存在未声明全局变量：`inventory`未使用`const/let/var`声明，意外挂载到window全局，容易造成命名冲突 | JS第1行`inventory = [];` | 改为`const inventory = [];` |
| 严重 | 存在空指针异常：恢复本地存储时，`localStorage.getItem`返回null时，`JSON.parse(null)`会得到null，但直接执行`JSON.parse(raw)`如果raw为null会抛出语法错误，导致整个页面脚本执行失败 | 自执行恢复函数`restore` | 添加空值判断：`if (!raw) return;`后再执行JSON.parse，包裹try-catch处理解析错误 |
| 严重 | 逻辑bug：数量未做类型转换，输入的数量是字符串，累加时会变成字符串拼接而不是数值相加，合计结果错误 | `showTotal`合计逻辑 | 将数量转为数值后再加：`total += Number(inventory[i].qty);`，同时添加数量非法输入校验 |
| 重要 | 未清理旧定时器：多次点击「开启自动保存」会创建多个重复定时器，造成多余的重复存储，浪费性能 | `autoSave`点击事件 | 声明一个变量保存定时器ID，每次点击前先清除旧定时器：`if (autoSaveTimer) clearInterval(autoSaveTimer);`再创建新定时器 |
| 重要 | 不符合JS编码规范：全部使用var和未声明变量，未使用ES6标准的`const/let` | 所有JS变量声明 | 将变量统一替换为`const`（不变变量）/`let`（可变变量），淘汰var和未声明变量 |
| 重要 | DOM操作不合理：每次添加日志都修改`innerHTML`，会导致整个日志区域重新解析渲染，日志多了后会有性能问题，同时丢失原有日志的事件绑定 | 所有日志拼接逻辑 | 使用独立文本节点添加新日志，或者提前构建完整HTML后一次性插入，使用`textContent`处理用户输入 |
| 重要 | MR描述为空，未说明变更目的和背景，不符合提交规范 | MR描述 | 补充MR描述，说明该文件的作用、变更背景 |
| 建议 | 事件绑定方式落后：使用`onclick`硬绑定，不支持多回调，可维护性差 | 所有DOM事件绑定 | 改用`addEventListener`绑定事件，推荐使用事件委托统一管理 |
| 建议 | 缺少必要的输入校验：没有校验品名/数量为空、数量非整数的情况，会导致无效数据入库 | `addSku`点击事件 | 添加非空判断、数字合法性校验，非法输入时给出提示 |
| 建议 | 缺少必要注释，核心逻辑没有说明 | JS逻辑部分 | 添加必要注释说明自动保存、数据恢复的逻辑 |

### 📊 总体评价
该MR存在多个严重级别的功能bug、安全问题和规范问题，文件命名不符合要求，MR描述不完整，**不建议合并**，修复上述严重问题后可重新提交评审。
