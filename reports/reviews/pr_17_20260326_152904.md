# AI 代码审查报告 PR #17

**PR 标题**: Create 222.html
**审核时间**: 2026-03-26 15:29:04

---

## 📝 GitLab MR 代码审核结果

### ✅ 优点
- 整体结构清晰，HTML语义化基础良好，viewport适配移动端
- 功能实现简单直观，符合单页演示需求
- 提供了本地持久化能力，用户体验有基本保障

### ⚠️ 问题/改进建议
| 严重程度 | 问题说明 | 位置 | 修复建议 |
|---------|---------|------|---------|
| 严重 | 文件名无意义，使用全数字`222.html`，不符合语义化命名规范 | 文件名 | 修改为`inventory-register.html`这类能描述页面功能的名称 |
| 严重 | MR描述为空，未说明本次提交的变更目的和背景，不满足协作要求 | MR描述 | 补充说明页面用途、变更背景，便于 reviewer 理解需求 |
| 严重 | 存在XSS安全漏洞：用户输入的品名直接通过`innerHTML`拼接注入到页面，攻击者可输入恶意脚本执行 | JS`addSku`、`showTotal`的日志拼接逻辑 | 禁止直接用innerHTML拼接用户输入，使用`textContent`处理用户内容，结合DOM创建拼接结构：<br>例如：`const textNode = document.createTextNode(\`已登记：${name} × ${qty}\`); log.appendChild(textNode); log.appendChild(document.createElement('br'));` |
| 严重 | 存在未处理的空值异常：恢复缓存时`localStorage.getItem('inv')`在无缓存时会返回`null`，调用`JSON.parse(null)`会抛出语法错误，导致整个页面JS执行失败 | 自执行函数`restore` | 先判断`raw`是否为非空值再解析：<br>`if (raw) { inventory = JSON.parse(raw); }` |
| 严重 | 存在意外全局变量：`inventory`未声明，直接挂载到window对象，容易引发变量污染冲突 | 所有使用`inventory`的位置 | 添加`let`声明：`let inventory = [];` |
| 严重 | 多次点击「开启自动保存」会创建多个重复定时器，造成无意义的重复执行，浪费性能 | `autoSave`点击回调 | 声明变量保存定时器ID，每次开启前清除旧定时器：<br>`let autoSaveTimer = null;`<br>点击逻辑内先`clearInterval(autoSaveTimer)`，再重新赋值新的定时器ID |
| 严重 | 数量计算逻辑错误：输入的`qty`是字符串类型，直接累加会导致字符串拼接而非数字求和（例：输入1和2会得到`"12"`而非`3`） | `showTotal`合计逻辑 | 转换为数字再累加：`total += Number(inventory[i].qty);`，同时补充输入合法性校验 |
| 重要 | 频繁修改`innerHTML`会导致每次拼接都重新解析渲染整个日志容器，引发不必要的重排重绘，日志多了后性能下降 | 所有日志拼接逻辑 | 使用DOM原生方法追加内容，避免全量重新解析DOM |
| 重要 | JS变量全部使用隐式全局/var，不符合现代JS规范，容易引发变量提升导致的逻辑问题 | 所有JS变量 | 统一替换为ES6的`const/let`：不变量用`const`，可变变量用`let`，移除var |
| 重要 | 使用`onclick`直接绑定DOM事件，不支持多回调绑定，可维护性差 | 所有事件绑定 | 替换为`addEventListener`绑定事件，使用事件委托进一步优化可维护性 |
| 重要 | 缺少输入合法性校验：品名和数量为空、数量非整数时依然允许入库，不符合业务逻辑 | `addSku`点击逻辑 | 提交前校验：品名不可为空，数量必须是合法正整数，非法输入提示用户 |
| 建议 | 内联样式缺少缩进统一，CSS选择器可以进一步优化可维护性 | style标签 | 统一缩进，可考虑给语义化class提升可读性 |
| 建议 | 缺少功能注释说明，尤其是核心逻辑对于新开发者不友好 | JS逻辑部分 | 补充简单注释说明自动保存、缓存恢复的逻辑 |

### 📊 总体评价
本次MR存在多个严重级别的功能错误、安全漏洞和规范问题，文件命名和MR描述也不符合要求，**当前不建议合并**，修复上述严重问题后可重新提交审核。
